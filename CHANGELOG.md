# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## v1.3.2（2026-09-15）

**修复：--init 模板的示例行会被当成真实记录导入**

- 问题：`--init` 生成的模板含 6 行未注释的示例记录，用户补完自己的记录直接导入时，示例行会被一并导入为真实工作记录（实测确认）。
- 方案：模板示例行全部加 `#` 注释（格式说明改为「去掉行首 # 并改成自己的记录」）；`--init` 后未补记录直接导入时，明确提示「没有解析到有效记录」而不是静默导入示例。
- 影响范围：仅 `scripts/import_manual.js` 的模板内容；已导入数据与既有流程不变。

## v1.3.1（2026-09-15）

**修复：导入的办公记录此前无法更正或删除（闭环漏洞）**

- 问题：导入按「日期 + 事项 + 仓库」只增不删，写错日期 / 事项 / 工时后，错误记录会永久留在 `out/commits.json`，没有任何更正路径。
- 方案：新增 `import_manual.js --sync` 对账模式——以记录文件为准重建全部办公记录（文件里删掉/改错的条目，台账同步删除/更新；`--sync --dry` 先预览新增/删除/更新清单）。默认行为仍为追加式导入，多文件场景不受影响。
- 文档：README 痛点表与 FAQ 补充更正路径；SKILL.md §3.4 流程与 §10 参数速查同步。

## v1.3.0（2026-09-15）

**补录工作不再需要自己编辑文件：把「主动询问 → 你口述 → 助手整理」写进流程**

- `import_manual.js` 新增 `--add "<日期 事项>"`：免编辑直接追加记录并导入，可重复；文件不存在时自动连同格式说明一起创建；重复内容与文件中完全相同的行都会自动跳过。`--add` 与 `--dry` 同用只预览、不写文件。
- SKILL.md §3.4 增加流程要求：汇总前**必须暂停询问**用户是否有不在 git 与办公软件里的工作要补（只需「时间 + 做了啥」，格式由执行者整理），落盘前先 `--dry` 让用户确认；用户答「没有」也要记录已确认。
- README「办公记录也能上账」改为对话式引导：直接跟助手说人话即可，命令行用户可用 `--add`；推荐提示词同步更新。

**文档**

- 标签精简为 8 个核心词（GitHub topics 同步收敛），并补充一句更直白的定位文案。

## v1.2.1（2026-09-14）

**流程修复：日期铺排的结果现在真正能进入建单**

- `zentao_sync.js` / `zentao_close.js` 新增 `--tree <路径>`：可直接使用 `out/task-tree-scheduled.json`（此前铺排结果无法被建单使用，只能手动改名替换）。`schedule_dates.js --apply` 现在会打印下一步提示。
- 修复 `zentao_sync.js` / `zentao_close.js` 未导入 `arg`，导致 `--all` 等参数从未被解析的问题；新增 `--all` 与 `--sample` 互斥校验（在读取任务树/连接浏览器之前即报错）。
- `portfolio.js` 明确「不带参数即执行校验」的语义（与 `--check` 等价）。

**文档**

- SKILL.md 新增「§10 命令行参数速查」表，覆盖全部脚本的命令行开关与预览约定。
- README 实测实录更新为含「会议 / 运维 + 人工工时」的混合运行结果；流水线补充 `import_manual` 步骤与铺排结果的使用方式。

## v1.2.0（2026-09-14）

**办公记录：覆盖更多办公场景，录入更方便**

- 支持手动录入规范工作记录：线下会议、运维值班、线上告警处理、培训支持等**没在任何软件里留痕**的工作，直接写进 `out/manual-work.md`，与办公软件 AI 总结同一格式、统一汇总。
- `import_manual.js --init`：一键生成记录模板（含格式说明与示例行，已存在则不覆盖）。
- 格式更宽容：`[模块名]` 与 `(2h)` 人工工时标注**顺序不限**；兼容行首列表符号（`-` `*` `·`）与 `（周X）`星期标注；日期支持 `YYYY-MM-DD` 或 `MM-DD`。
- 新增 `(2h)` / `（2小时）` 人工指定工时：任务全部来源记录都带工时标注时，按人工合计计（诚实口径，审计可查），`check_estimate.js` 对此类任务不做难度偏差校验（W2），但单任务 / 单日上限照常校验。
- README 补充「行格式一览表」与混合实测输出。

## v1.1.0（2026-09-14）

**新功能：办公记录也能上账**

- 新增 `scripts/import_manual.js`：把企业微信 / 钉钉 / 飞书等办公平台 AI 生成的月度工作总结（需求评审、接口/文档对接、联调支持、部署运维、方案与文档编写等非代码工作）并入 `out/commits.json`，与代码提交一起聚类、定档、铺排。
- 导入记录带 `source:"manual"` 标记，任务描述以**【办公记录】**呈现（日期 + 事项原文），不伪造 commit hash；幂等去重，重复导入自动跳过。
- `collect_commits.js` 重采时保留已导入的办公记录（零破坏）。
- 成果话术库补充评审 / 文档 / 部署 / 沟通类表述；多档位命中的判定理由改用顿号合并，修复描述断裂。
- 无本地仓库也能用：跳过采集，仅导入办公记录即可走完整流水线。

**文档**

- README 新增「办公记录也能上账」使用指南、对应推荐提示词与办公记录任务示例；实测实录更新为「7 次提交 + 4 条办公记录」的混合运行结果。

## v1.0.0（2026-09-14）

首个公开版本。

**核心能力**

- 全流程 10 阶段流水线，每阶段可独立执行、断点续跑、幂等：环境体检 → 目标定位 → 提交采集 → 需求汇总 → 工时校验 → 日期铺排 → 存量补写/重排/收敛 → 禅道建单 → 任务闭环 → 全项目台账。
- 提交采集支持本地 `git log`（零凭据）与 Gitea / GitHub / Gitee API。
- 三层任务结构：月份父任务 → 模块子任务 → 功能点叶子任务，自动补写禅道 `parent` 层级。
- 难度阶梯工时 T1~T7（1/2/3/4/6/8/10~24h），命中理由写入任务描述的【工时依据】。
- 工时硬约束校验：单任务上下限（E1）、单日折算上限（E2）、月度目标（E3）、全项目月度合计上限（E4），以及难度偏差（W2）与粒度告警（W3）。
- 日期铺排引擎：按工时决定占用天数并锚定真实提交日，自动消除单日峰值。
- 存量任务复用：补写、重排、消耗收敛，同一批工作只计一次工时。
- 任务描述双视角生成：交付成果（领导/甲方视角）＋ 来源提交与工时依据（同事/审计视角）。

**配置与定制**

- `estimate.tierRules`：按技术栈自定义 T1~T7 判定关键词，支持追加或整档替换。
- `collect.moduleAliases`：把提交 scope / 路径映射为业务模块名。
- `workload` / `portfolio`：月度目标与全项目总量上限。

**工程说明**

- 纯 Node.js 实现（≥ 18），唯一依赖 `playwright-core`，无构建步骤。
- 禅道交互通过 CDP 接管已登录浏览器，账号密码不经过脚本。
- 仓库内所有示例均为占位内容，不含任何真实项目或个人信息；`config.json`、`out/`、`portfolio.json` 均在 `.gitignore` 中。
- MIT 许可。
